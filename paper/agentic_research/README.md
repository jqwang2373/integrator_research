# agentic_research — the process paper

**"Agentic auto-research in numerical analysis: a coding agent finds, proves and validates a
sixth-order Lie-group integrator."** A case study of how the Gauss6/FullVA program in this
repository was carried out by a coding agent under human direction: the process architecture
(versioned tree, evidence ledger, proof sandbox, memory, command boundaries, manuscript
pipeline), the trajectory through the 49 versions, ten incidents in which a wrong claim was
caught and by what, and a quantitative account of the final session. ACL Rolling Review /
NAACL template (`acl.sty`, two columns, `preprint` option): eight pages of main text (the ACL
long-paper limit for review versions), then the mandatory Limitations section, an Ethics
Statement, references and three appendices, all outside the page count.

| File | What it is |
| --- | --- |
| `main.tex` | Preamble, title, abstract; inputs `sections/01_intro … 09_conclusion`, `10_limitations`, `11_ethics` and `sections/appendix`. Switch `\usepackage[preprint]{acl}` to `review` for an anonymous submission with line numbers, `final` for camera-ready. |
| `sections/*.tex` | The paper, one file per section. |
| `references.bib`, `acl.sty`, `acl_natbib.bst` | Bibliography and the official ACL style files (from github.com/acl-org/acl-style-files, fetched 2026-09-21). |
| `make_figures.py` | Draws `figures/fig_versions.png` (from `../../numerics/v0*`), `figures/fig_session.png` (from the Claude Code transcript under `~/.claude/projects/`), `figures/fig_pipeline.png` (schematic); writes `stats.tex`, `session_stats.json`, `version_table.csv`. |
| `stats.tex` | Macros `\nHuman`, `\nTurns`, `\nTools`, `\nEntries`, `\nHarness`, `\nShell` read by `main.tex`; regenerated by `make_figures.py`. |
| `main.pdf` | Compiled paper. |

## Build

```bash
cd paper/agentic_research
../../.venv_sbel/bin/python make_figures.py                                  # figures + stats.tex
cmd.exe /c "latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex"   # Windows TeX from WSL
```

## Provenance of the numbers

* Repository counts (Table 2) were taken from the working tree on 2026-09-21; the session
  counts come from the transcript of the final session (2026-09-17 to 21). The transcript keeps
  growing while that session is open, so `\nTurns`/`\nTools` move; `\nHuman` counts only messages
  typed by the human (harness-generated user-role entries such as task notifications, command
  echoes, compaction summaries and image attachments are excluded, and typed shell commands are
  counted separately as `\nShell`).
* The classification of the human messages in Appendix C was done by hand on the 36 messages
  present on 2026-09-21; re-check it if `\nHuman` changes.
* The incident list (Table 1) is reconstructed from the transcript and the ledger records of the
  final phase; it is complete for that phase only.

## NAACL 2027 (checked 2026-09-21 against the CFP and the ARR policy pages)

* Submissions go through ACL Rolling Review; the ARR deadline for the NAACL 2027 cycle is
  **2026-10-12**, commitment 2026-12-23, notification 2027-02-10, camera-ready 2027-03-03,
  conference 2027-06-01 to 05 (San Francisco).
* Long papers: 8 pages of content in the review version, 9 in the final version; references,
  the mandatory `Limitations` section, the optional `Ethical considerations` section and the
  appendices do not count, provided they come after the conclusion.
* The Responsible NLP Research Checklist must be filled in at submission; use of generative AI in
  writing or coding must be disclosed there and detailed in the Acknowledgments (done in
  `sections/11_ethics.tex`; keep that section in the review version, it does not reveal identity).
* No anonymity period: non-anonymous preprints are allowed during review (anonymous submissions get
  priority for borderline decisions). The theme track is "Language as a Medium for Agentic
  Communication".

## Submission notes

* Use the `review` option for an anonymous submission (line numbers, no author block); the
  companion-manuscript citation, the GitHub remotes mentioned in Section 4.5 and the memory-file
  descriptions in Appendix B would need masking.
* Page budget: the conclusion ends at the bottom of page 8, so any sentence added to Sections 1–9
  must be paid for elsewhere; Limitations, Ethics Statement, references and appendices do not count.
* Wide floats are `figure*`/`table*` (they land at the top of the following page); the artefacts
  table is single-column. ACL puts table captions below the table.
* An earlier NeurIPS-style build of the same text is in git history (commit `ad63f6b`).
* The paper was drafted by the agent from the repository and the transcript; the author is
  responsible for its content (stated in Section 8).
