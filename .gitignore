# local shallow clone of https://github.com/uwsbel/public-metadata (no working tree); restore with git clone
external/public-metadata/
# Python caches and the project virtual environment (uv, Python 3.11; see validation/README.md)
__pycache__/
*.pyc
.venv_sbel/
# LaTeX build byproducts (logs and PDFs stay tracked because validators read them)
*.aux
*.fls
*.fdb_latexmk
*.spl
*.synctex.gz
*.nav
*.snm
*.toc
paper/**/*.out
paper/*.out
validation/legacy_drafts/*.out
